{#-
  Crawler directives.

  The `Sitemap:` line is the reason this file exists at all — without it a crawler has no way to
  find the sitemap short of someone submitting it by hand.

  Nothing is disallowed. A static site has no admin path to hide, and a `Disallow` here would not
  hide one anyway: the file is public and is read by people looking for exactly that.
-#}
User-agent: *
Allow: /

Sitemap: {{ (site.base_url ~ "/sitemap.xml") | safe }}
